# 域名部署完整指南

## 📋 部署方案概览

将 openEar 部署到域名上，让别人可以访问，主要有以下几个步骤：

1. **购买域名**
2. **准备服务器**（云服务器/VPS）
3. **部署应用**
4. **配置 DNS**
5. **配置 HTTPS（SSL证书）**
6. **配置 Nginx 反向代理**

---

## 第一步：购买域名

### 推荐域名注册商

1. **国内**：
   - 阿里云（万网）：https://wanwang.aliyun.com
   - 腾讯云：https://dnspod.cloud.tencent.com
   - 华为云：https://www.huaweicloud.com/product/domain.html

2. **国外**：
   - Namecheap：https://www.namecheap.com
   - GoDaddy：https://www.godaddy.com
   - Cloudflare：https://www.cloudflare.com/products/registrar/

### 购买步骤

1. 在注册商网站搜索想要的域名（如：`openear.com`、`openear.cn`）
2. 选择域名并完成购买
3. 完成实名认证（国内域名需要）

**费用参考**：
- `.com` 域名：约 50-100 元/年
- `.cn` 域名：约 30-50 元/年
- `.xyz` 域名：约 10-20 元/年

---

## 第二步：准备服务器

### 服务器选择

#### 方案1：云服务器（推荐）

**国内云服务商**：
- **阿里云 ECS**：https://www.aliyun.com/product/ecs
  - 最低配置：1核2G，约 100-200 元/月
  - 推荐配置：2核4G，约 300-500 元/月
- **腾讯云 CVM**：https://cloud.tencent.com/product/cvm
- **华为云 ECS**：https://www.huaweicloud.com/product/ecs.html

**国外云服务商**：
- **DigitalOcean**：https://www.digitalocean.com（$6/月起）
- **Linode**：https://www.linode.com（$5/月起）
- **Vultr**：https://www.vultr.com（$6/月起）
- **AWS EC2**：https://aws.amazon.com/ec2/
- **Google Cloud Platform**：https://cloud.google.com

#### 方案2：VPS（虚拟专用服务器）

适合有一定技术基础的用户，价格相对便宜。

### 服务器配置要求

**最低配置**：
- CPU：1核
- 内存：2GB
- 硬盘：20GB SSD
- 带宽：1-3Mbps
- 操作系统：Ubuntu 20.04/22.04 LTS（推荐）

**推荐配置**：
- CPU：2核
- 内存：4GB
- 硬盘：40GB SSD
- 带宽：5Mbps
- 操作系统：Ubuntu 22.04 LTS

### 购买后操作

1. **获取服务器 IP 地址**
2. **使用 SSH 连接服务器**：
   ```bash
   ssh root@你的服务器IP
   ```
3. **更新系统**：
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

---

## 第三步：部署应用

### 1. 在服务器上安装依赖

```bash
# 安装 Python 3.10+
sudo apt install python3 python3-pip python3-venv -y

# 安装其他依赖
sudo apt install nginx git -y

# 安装 pydub 的依赖（用于音频处理）
sudo apt install ffmpeg -y
```

### 2. 上传项目代码

**方法1：使用 Git（推荐）**

```bash
# 在服务器上克隆项目
cd /var/www
git clone https://github.com/你的用户名/openEar.git
cd openEar/openEar
```

**方法2：使用 SCP 上传**

在本地电脑执行：
```bash
scp -r /Users/liuzirui/Desktop/openEar root@你的服务器IP:/var/www/
```

### 3. 配置项目

```bash
cd /var/www/openEar/openEar

# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip install -r requirements_prod.txt

# 创建必要的目录
mkdir -p logs
mkdir -p instance
```

### 4. 配置数据库

```bash
# 初始化数据库
source venv/bin/activate
python3 -c "from app import app, db; app.app_context().push(); db.create_all()"
```

### 5. 测试运行

```bash
# 使用 Gunicorn 测试
source venv/bin/activate
gunicorn -c gunicorn_config.py app:app
```

如果看到服务器启动成功，按 `Ctrl+C` 停止。

---

## 第四步：配置 Nginx 反向代理

### 1. 创建 Nginx 配置文件

```bash
sudo nano /etc/nginx/sites-available/openear
```

添加以下内容：

```nginx
server {
    listen 80;
    server_name 你的域名.com;  # 替换为你的域名

    # 静态文件
    location /static {
        alias /var/www/openEar/openEar/static;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # 反向代理到 Gunicorn
    location / {
        proxy_pass http://127.0.0.1:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket 支持（如果需要）
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 文件大小限制（用于音频上传）
    client_max_body_size 50M;
}
```

### 2. 启用配置

```bash
# 创建软链接
sudo ln -s /etc/nginx/sites-available/openear /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重启 Nginx
sudo systemctl restart nginx
```

---

## 第五步：配置 DNS

### 在域名注册商处配置 DNS

1. **登录域名注册商控制台**
2. **找到 DNS 解析设置**
3. **添加 A 记录**：
   - **记录类型**：A
   - **主机记录**：@（或留空，表示主域名）
   - **记录值**：你的服务器 IP 地址
   - **TTL**：600（10分钟）

4. **添加 www 子域名（可选）**：
   - **记录类型**：A
   - **主机记录**：www
   - **记录值**：你的服务器 IP 地址
   - **TTL**：600

### DNS 生效时间

- 通常 5-30 分钟生效
- 最长可能需要 24-48 小时
- 可以使用 `ping 你的域名.com` 检查是否生效

---

## 第六步：配置 HTTPS（SSL 证书）

### 使用 Let's Encrypt 免费证书

```bash
# 安装 Certbot
sudo apt install certbot python3-certbot-nginx -y

# 获取证书（自动配置 Nginx）
sudo certbot --nginx -d 你的域名.com -d www.你的域名.com

# 按照提示输入邮箱等信息
```

### 自动续期

Let's Encrypt 证书有效期 90 天，Certbot 会自动续期。可以测试续期：

```bash
sudo certbot renew --dry-run
```

### 手动配置 HTTPS（如果需要）

如果自动配置失败，可以手动编辑 Nginx 配置：

```bash
sudo nano /etc/nginx/sites-available/openear
```

添加 HTTPS 配置：

```nginx
server {
    listen 443 ssl http2;
    server_name 你的域名.com;

    ssl_certificate /etc/letsencrypt/live/你的域名.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/你的域名.com/privkey.pem;

    # SSL 配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # 其他配置同 HTTP
    location / {
        proxy_pass http://127.0.0.1:5001;
        # ... 其他配置
    }
}

# HTTP 重定向到 HTTPS
server {
    listen 80;
    server_name 你的域名.com;
    return 301 https://$server_name$request_uri;
}
```

---

## 第七步：配置系统服务（开机自启）

### 创建 systemd 服务

```bash
sudo nano /etc/systemd/system/openear.service
```

添加以下内容：

```ini
[Unit]
Description=openEar Gunicorn daemon
After=network.target

[Service]
User=www-data  # 或你的用户名
Group=www-data
WorkingDirectory=/var/www/openEar/openEar
Environment="PATH=/var/www/openEar/openEar/venv/bin"
ExecStart=/var/www/openEar/openEar/venv/bin/gunicorn -c gunicorn_config.py app:app
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
```

### 启动服务

```bash
# 重新加载 systemd
sudo systemctl daemon-reload

# 启动服务
sudo systemctl start openear

# 设置开机自启
sudo systemctl enable openear

# 查看服务状态
sudo systemctl status openear
```

---

## 第八步：配置防火墙

### 使用 UFW（Ubuntu）

```bash
# 允许 SSH（重要！）
sudo ufw allow 22/tcp

# 允许 HTTP 和 HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# 启用防火墙
sudo ufw enable

# 查看状态
sudo ufw status
```

### 云服务器安全组配置

如果使用云服务器，还需要在控制台配置安全组：

1. 登录云服务器控制台
2. 找到"安全组"设置
3. 添加入站规则：
   - **端口**：80（HTTP）
   - **端口**：443（HTTPS）
   - **端口**：22（SSH，仅限你的IP）

---

## 验证部署

### 1. 检查服务状态

```bash
# 检查 Gunicorn
sudo systemctl status openear

# 检查 Nginx
sudo systemctl status nginx

# 查看日志
sudo journalctl -u openear -f
tail -f /var/www/openEar/openEar/logs/error.log
```

### 2. 访问网站

在浏览器中访问：
- `http://你的域名.com`（应该自动跳转到 HTTPS）
- `https://你的域名.com`

### 3. 测试功能

- 注册/登录
- 音程识别练习
- 音阶练习
- 和弦识别练习

---

## 常见问题排查

### 1. 无法访问网站

```bash
# 检查 Nginx 状态
sudo systemctl status nginx

# 检查端口是否监听
sudo netstat -tlnp | grep :80
sudo netstat -tlnp | grep :443

# 检查防火墙
sudo ufw status

# 查看 Nginx 错误日志
sudo tail -f /var/log/nginx/error.log
```

### 2. 502 Bad Gateway

```bash
# 检查 Gunicorn 是否运行
sudo systemctl status openear

# 检查端口 5001
sudo netstat -tlnp | grep :5001

# 重启服务
sudo systemctl restart openear
```

### 3. 静态文件无法加载

```bash
# 检查静态文件目录权限
sudo chown -R www-data:www-data /var/www/openEar/openEar/static
sudo chmod -R 755 /var/www/openEar/openEar/static
```

### 4. 数据库错误

```bash
# 检查数据库文件权限
sudo chown -R www-data:www-data /var/www/openEar/openEar/instance
sudo chmod -R 755 /var/www/openEar/openEar/instance
```

---

## 性能优化建议

### 1. 增加 Gunicorn Workers

编辑 `gunicorn_config.py`：

```python
workers = multiprocessing.cpu_count() * 2 + 1
```

### 2. 启用 Nginx 缓存

在 Nginx 配置中添加：

```nginx
# 缓存静态文件
location ~* \.(jpg|jpeg|png|gif|ico|css|js|mp3|wav)$ {
    expires 30d;
    add_header Cache-Control "public, immutable";
}
```

### 3. 使用 CDN（可选）

对于静态资源（音频文件），可以考虑使用 CDN：
- 阿里云 CDN
- 腾讯云 CDN
- Cloudflare（免费）

---

## 备份和维护

### 定期备份

```bash
# 创建备份脚本
nano /root/backup_openear.sh
```

```bash
#!/bin/bash
BACKUP_DIR="/root/backups/openear"
DATE=$(date +%Y%m%d_%H%M%S)
mkdir -p $BACKUP_DIR

# 备份代码
tar -czf $BACKUP_DIR/code_$DATE.tar.gz /var/www/openEar

# 备份数据库
cp /var/www/openEar/openEar/instance/*.db $BACKUP_DIR/db_$DATE.db

# 删除 7 天前的备份
find $BACKUP_DIR -name "*.tar.gz" -mtime +7 -delete
find $BACKUP_DIR -name "*.db" -mtime +7 -delete
```

```bash
# 添加执行权限
chmod +x /root/backup_openear.sh

# 添加到 crontab（每天凌晨 2 点备份）
crontab -e
# 添加：0 2 * * * /root/backup_openear.sh
```

### 更新代码

```bash
cd /var/www/openEar/openEar
git pull
source venv/bin/activate
pip install -r requirements_prod.txt
sudo systemctl restart openear
```

---

## 成本估算

### 最低配置（适合个人/小规模使用）

- **域名**：50 元/年
- **服务器**：100-200 元/月（1核2G）
- **总计**：约 1250-2450 元/年

### 推荐配置（适合正式使用）

- **域名**：50 元/年
- **服务器**：300-500 元/月（2核4G）
- **SSL 证书**：免费（Let's Encrypt）
- **总计**：约 3650-6050 元/年

---

## 安全建议

1. **定期更新系统**：
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

2. **使用强密码**：SSH、数据库等

3. **禁用 root 登录**（可选）：
   ```bash
   sudo nano /etc/ssh/sshd_config
   # 设置：PermitRootLogin no
   sudo systemctl restart sshd
   ```

4. **定期备份数据**

5. **监控日志**：定期查看错误日志

6. **使用 Fail2ban**（防止暴力破解）：
   ```bash
   sudo apt install fail2ban -y
   sudo systemctl enable fail2ban
   ```

---

## 总结

完成以上步骤后，你的 openEar 应用就可以通过域名访问了！

**快速检查清单**：
- [ ] 域名已购买并配置 DNS
- [ ] 服务器已准备并安装依赖
- [ ] 代码已上传并配置
- [ ] Nginx 已配置并运行
- [ ] SSL 证书已安装
- [ ] 系统服务已配置并自启
- [ ] 防火墙已配置
- [ ] 网站可以正常访问

如有问题，可以查看日志文件或联系技术支持。

